<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Manofthematch.LandingPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Clicked="GetContent" Text="Klik"></Button>
            <ScrollView>
                <ListView> <!--ItemsSource="{Binding Flags}" 
                          SelectedItem="{Binding CurrentFlag, Mode=TwoWay}"
                          ItemTapped="OnItemTapped"
                          IsPullToRefreshEnabled="True"
                          Refreshing="OnRefreshing" -->
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ImageCell/> <!--DetailColor="Gray"
                           ImageSource="{Binding SmallImageUrl, Mode=OneWay, Converter={StaticResource irConverter}}"
                           Text="{Binding Country}"
                           Detail="{Binding DateAdopted, StringFormat='Adopted on {0:d}'}"--> 
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>